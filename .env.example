# --- Database ---
DATABASE_URL="postgresql://cosmic:cosmic@localhost:5432/cosmic_daily?schema=public"

# --- Auth (Auth.js / NextAuth) ---
# Generate with: npx auth secret
AUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (optional). If unset, the Google login button is hidden.
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# --- Geocoding ---
# Free, no key required: OpenStreetMap Nominatim is used by default.
# Set GEOCODE_USER_AGENT to something identifying your app per Nominatim usage policy.
GEOCODE_USER_AGENT="horospaper-dev/1.0"

# --- LLM (horoscope text generation) ---
# Set one of these. If neither is set, a deterministic template-based
# fallback generator is used instead (clearly logged as MOCK mode).
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""
LLM_PROVIDER="anthropic" # "anthropic" | "openai" | "mock"

# --- Image generation ---
# If unset, a procedural SVG "cosmic portrait" is generated locally instead
# of calling an external image API (MOCK mode).
IMAGE_PROVIDER="mock" # "openai" | "mock"
# OPENAI_API_KEY is reused for image generation when IMAGE_PROVIDER="openai"

# --- Storage ---
# "local" stores generated images under /public/generated and serves them
# directly from Next.js. Set to "s3" to use S3-compatible object storage.
STORAGE_PROVIDER="local" # "local" | "s3"
S3_ENDPOINT=""
S3_REGION=""
S3_BUCKET=""
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
S3_PUBLIC_BASE_URL=""

# --- Rate limiting ---
FREE_GENERATIONS_PER_DAY="1"
# Local development only: bypass daily generation limits.
DISABLE_USAGE_LIMITS="false"
